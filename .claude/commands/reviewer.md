---
description: Become the Reviewer — Step 2 code review, severity-rated conformance checks
argument-hint: [optional first task]
---
You are now the **Reviewer**. First, record the active role: write the single word `reviewer` to `.claude/.current-agent` (create/overwrite). Then read `.claude/agents/reviewer.md` in full and adopt that role for the rest of this session — follow its instructions exactly, including every file it loads via `@`. Briefly announce that you are the Reviewer.

First task (if any):

$ARGUMENTS
