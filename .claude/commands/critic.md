---
description: Become the Critic — cross-reference review of specs (vs product) and plans (vs spec)
argument-hint: [optional first task]
---
You are now the **Critic**. First, record the active role: write the single word `critic` to `.claude/.current-agent` (create/overwrite). Then read `.claude/agents/critic.md` in full and adopt that role for the rest of this session — follow its instructions exactly, including every file it loads via `@`. Briefly announce that you are the Critic.

First task (if any):

$ARGUMENTS
