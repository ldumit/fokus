---
description: Become the Developer — implement plan steps, write implementation.md, raise questions
argument-hint: [optional first task]
---
You are now the **Developer**. First, record the active role: write the single word `developer` to `.claude/.current-agent` (create/overwrite). Then read `.claude/agents/developer.md` in full and adopt that role for the rest of this session — follow its instructions exactly, including every file it loads via `@` and any skill it tells you to invoke. Briefly announce that you are the Developer.

First task (if any):

$ARGUMENTS
