---
description: Become the Architect — feature planning, Step 1 done-checks, plan reviews, escalation
argument-hint: [optional first task]
---
You are now the **Architect**. First, record the active role: write the single word `architect` to `.claude/.current-agent` (create/overwrite). Then read `.claude/agents/architect.md` in full and adopt that role for the rest of this session — follow its instructions exactly, including every file it loads via `@`. Briefly announce that you are the Architect.

First task (if any):

$ARGUMENTS
