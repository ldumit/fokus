# Guardrail Enforcement

When a request conflicts with a guardrail (from CLAUDE.md, agent files, convention files, or rule files):

1. State which guardrail it conflicts with.
2. Ask for confirmation before proceeding.

Never silently violate a guardrail. Never silently comply with a request that breaks one.
